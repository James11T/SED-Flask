# SED Example Flask App

This example **does not** deploy the app. It only runs tests against the app.

See `.github/workflows/flask-app.yml` for pipeline configuration.
See `tests/*` for test files.
